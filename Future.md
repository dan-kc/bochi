When "completing a habit/reward" from the change forms, it should close the change form. Remove the "Save" button from the change forms, it should just save automatically (when the user focuse out or something). 

Also add a swipe gesture to the item in list view. Swiping left should complete a habit / reward and notify the user with some temporary color change or something give me options

---

Make the UI more modern and minimal for the habit/reward change forms, requiring more clicks to accomplish things, but making it look much better.

I have attatched some screenshots of how I want this to look on mobile/tablet vs desktop. This is from the todoist app. This has many concepts that I don't such as comments and priority etc. Ignore those and instead use the concepts I use.

The desktop screenshots particularly show the flow of creating/changing tags which I think is much cleaner for todoist and I want to implement for mine.

On mobile/tablet note how each field (with fields denoted by the thin line seperator) when you tap on it, it brings up a new modal (animated from bottom just like the change form) for changing just that one field. I like this as it minimises the UI.

Note the tags flow on mobile too, very minimal.

Note that there is a list of buttons near the bottom. "Reminders", "Deadline" etc... Things appear in this list if they are Null values. This keeps things evern more clean, all Null values are just in this scrollable list. If i click on the button it will bring up the form for adding a value for this field (which is the same as the change form for this field)

Ensure the "new habit/reward" button is consistent with the change form, they should look identical really, except the change form also has the history.
