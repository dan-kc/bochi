I am writing a native IOS version (./ios) of my existing react-native frontend (./frontend).

Implement the Habits page. It should have a "+" Icon in the bottom right for adding a new habit and it should pop up with a form. Make this local-only for now, do not implement the local-first sync layer sound in the old project.

Do not copy any styling. Use default styling for SwiftUI including icons etc.
Use strict TDD, always write unit tests first. No UI tests.
I will run tests manually for validation, do not run any xcode commands - notify if you need me to run any.

# NEXT

Add the price 

Also ensure that this pill scroll correctly conveys the case where there is overflow.
