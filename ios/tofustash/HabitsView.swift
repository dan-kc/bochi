import SwiftUI

struct HabitsView: View {
    var body: some View {
        NavigationStack {
            Color.clear
                .navigationTitle("Habits")
        }
    }
}

#Preview {
    HabitsView()
}
