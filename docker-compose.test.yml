services:
  db:
    volumes:
      - test_data:/var/lib/postgresql/data

  # server:
  #   ports: []

  test_suite:
    build: ./test_suite
    # depends_on:
    #   server:
    #     condition: service_healthy

volumes:
  test_data:
